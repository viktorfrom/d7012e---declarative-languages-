module Lab1
    ( smallest_k_sets
    ) where

smallest_k_sets :: (Integral a) => [a]
smallest_k_sets = result

list :: (Integral a) => [a]
list = [1,2,3]

result :: (Integral a) => [a]
result = list
